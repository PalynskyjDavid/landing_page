# Optional: copy to frontend/.env.local for local overrides.
# PUBLIC build/dev-server configuration. Never put secrets in VITE_* variables.
VITE_API_URL=http://localhost:3001
# Only for an explicitly configured development same-origin proxy:
# VITE_API_URL=/api
# VITE_API_PROXY_TARGET=http://localhost:3001
